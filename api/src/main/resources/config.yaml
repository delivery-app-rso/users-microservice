kumuluzee:
  name: items-microservice-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/ItemDS
      connection-url: jdbc:postgresql://postgres:5432/items-db
      username: dbuser
      password: postgres
      max-pool-size: 20
